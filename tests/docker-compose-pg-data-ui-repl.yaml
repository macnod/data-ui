services:
  pg-data-ui-repl:
    image: postgres:16
    pull_policy: missing
    environment:
      POSTGRES_HOST: "$DB_HOST"
      POSTGRES_DATABASE: "$DB_NAME"
      POSTGRES_USER: "$DB_USER"
      POSTGRES_PASSWORD: "$DB_PASSWORD"
    ports:
      - "${DB_PORT}:5432"
