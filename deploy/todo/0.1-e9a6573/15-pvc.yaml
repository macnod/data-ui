# Generated by scripts/run.sh deploy -- instance todo (todo-0.1-e9a6573)
#
# PVC names are stable across versions so a new image rollout reuses the
# existing volumes.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dataui-todo-db
  namespace: dataui-todo
  labels:
    data-ui/instance: todo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  volumeName: dataui-todo-demo-db-pv
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dataui-todo-files
  namespace: dataui-todo
  labels:
    data-ui/instance: todo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  volumeName: dataui-todo-demo-files-pv
  resources:
    requests:
      storage: 5Gi
