# Generated by scripts/run.sh deploy -- instance todo (todo-0.1-e9a6573)
#
# hostPath volumes live inside the k3d node container. They survive pod
# restarts and image rollouts, but not deletion of the k3d cluster.
# NOTE: multiple replicas would require ReadWriteMany (e.g. EFS) on a
# real cluster later.
apiVersion: v1
kind: PersistentVolume
metadata:
  name: dataui-todo-demo-db-pv
  labels:
    data-ui/instance: todo
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: /data/dataui/todo-demo/db
    type: DirectoryOrCreate
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: dataui-todo-demo-files-pv
  labels:
    data-ui/instance: todo
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  hostPath:
    path: /data/dataui/todo-demo/files
    type: DirectoryOrCreate
